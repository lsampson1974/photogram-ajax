
var existingRequests = $(".unfollow_form_<%= @follow_request.recipient_id %>");

var updatedRequests = "<%= j(render "follow_requests/follow_unfollow", sender: @follow_request.sender_id, recipient: @follow_request.recipient) %>";

$(existingRequests).replaceWith(updatedRequests);
